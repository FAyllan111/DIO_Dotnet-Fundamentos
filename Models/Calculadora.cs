
/*
namespace dio_dotnet_bootcamp.Models
{
     
    public class Calculadora
    {   
        public void Somar(int x, int y)
        {
            //Console.WriteLine($"{x}+{y} ={x + y}");
        }

        public void Subtrair(int x,int y)
        {
            //Console.WriteLine($"{x}-{y} = {x - y}");
        }

        public void Multiplicar(int x, int y)
        {
           // Console.WriteLine($"{x} X {y}={x * y}");
        }

        public void Dividir(int x,int y)
        {
            //Console.WriteLine($"{x} / {y} = {x / y}");
        }

        public void Potencia(int x, int y)
        {
            double pot = Math.Pow(x, y);
            //Console.WriteLine($"{x} ^{y} = {pot}");
        }

        public void Seno(double angulo)
        {
            double rad = angulo * Math.PI / 180;
            double seno = Math.Sin(rad);
            //Console.WriteLine($"Seno é:{Math.Round(seno,4)}");
        }
        
        public void Cosseno(double angulo)
        {
            double rad = angulo * Math.PI / 180;
            double cosseno = Math.Cos(rad);
            //Console.WriteLine($"Cosseno é:{Math.Round(cosseno,4)}");
        }
        
        public void Tangente(double angulo)
        {
            double rad = angulo * Math.PI / 180;
            double tangente = Math.Tan(rad);
           // Console.WriteLine($"Tangente é:{Math.Round(tangente,4)}");
        }
    }
}
*/